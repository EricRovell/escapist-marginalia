name: build

on:
  push:
    paths-ignore:
      - "**/README.md"
  pull_request:
    paths-ignore:
      - "**/README.md"
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - name: install-node-js
        uses: actions/setup-node@master
        with:
          node-version: 22.x
      - name: install-pnpm
        run: npm i pnpm -g
      - name: install-dependencies
        run: pnpm i -r
      - name: eslint
        run: pnpm run lint:code
      - name: stylelint
        run: pnpm run lint:styles
      - name: typescript
        run: pnpm run lint:types
      - name: unit-testing
        run: pnpm run test:unit:ci
      - name: unit-testing-coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: ./coverage
          if-no-files-found: error
      - name: build-en
        run: pnpm run build
      - name: build-en-artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-en
          path: ./src/packages/escapist-marginalia/dist
          if-no-files-found: error
      - name: build-ru
        run: LANG=ru pnpm run build
      - name: build-ru-artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-ru
          path: ./src/packages/escapist-marginalia/dist-ru
          if-no-files-found: error
